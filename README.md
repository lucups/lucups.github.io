# Tony.Engineer

Personal homepage for [tony.engineer](https://tony.engineer).

The page is generated from the configuration and template in `make.php`:

```sh
php make.php
```

`master` contains the source. GitHub Pages publishes the generated static files
from `gh-pages`.
